/**
 * Greets a user with a welcome message.
 * @param name The name of the person to greet
 * @returns A formatted greeting string
 */
export function greet(name: string): string {
  return `Hello, ${name}! Welcome to Webby.`;
}